@{
    ViewData["Title"] = "Добро пожаловать в музей";
}

@section Styles {
    <style>
        .museum-welcome {
            margin-top: 60px;
            margin-bottom: 40px;
        }
        .museum-welcome h1 {
            color: #473e2a;
            font-weight: 800;
            letter-spacing: 0.03em;
            text-shadow: 0 2px 8px #e5decf;
        }
        .museum-welcome .lead {
            color: #7b6e59;
            font-weight: 600;
            font-size: 1.5em;
        }

        .museum-cards-row {
            margin-top: 40px;
            display: flex;
            justify-content: center;
            gap: 32px;
        }

        .museum-card {
            background: linear-gradient(135deg, #ede3d1 0%, #cbb086 100%);
            border-radius: 18px;
            box-shadow: 0 8px 32px rgba(71, 62, 42, 0.16);
            border: none;
            transition: transform 0.18s, box-shadow 0.18s;
            min-width: 260px;
            max-width: 320px;
            flex: 1 1 260px;
            display: flex;
            flex-direction: column;
            align-items: center;
            padding: 36px 24px 28px 24px;
        }
        .museum-card:hover {
            transform: translateY(-8px) scale(1.03);
            box-shadow: 0 12px 36px rgba(71,62,42,0.22);
            background: linear-gradient(135deg, #e5decf 0%, #bfa77a 100%);
        }
        .museum-card .card-title {
            font-size: 1.45em;
            color: #473e2a;
            font-weight: 700;
            margin-bottom: 14px;
            letter-spacing: 0.01em;
        }
        .museum-card .card-text {
            font-size: 1.08em;
            color: #5e4e30;
            margin-bottom: 18px;
            font-weight: 500;
        }
        .museum-card .btn {
            background-color: #bfa77a;
            border: none;
            font-weight: 700;
            letter-spacing: 0.03em;
            box-shadow: 0 2px 10px #e5decf99;
            border-radius: 8px;
            padding: 10px 28px;
            transition: background 0.2s, color 0.2s;
        }
        .museum-card .btn:hover {
            background-color: #473e2a;
            color: #f6eddc;
        }
        
        
    </style>
}

<div class="museum-welcome text-center">
    <h1 class="display-4">Добро пожаловать в сервис для инвентаризации музейных экспонатов</h1>
    <p class="lead">Чувашский государственный художественный музей</p>
</div>

<div class="container">
    <div class="museum-cards-row">
        <div class="museum-card">
            <div class="card-body">
                <h5 class="card-title">Экспонаты</h5>
                <p class="card-text">Полный список музейных экспонатов, их описание и историю.</p>
                <a asp-controller="Exhibits" asp-action="Index" class="btn btn-primary">Экспонаты</a>
            </div>
        </div>
        <div class="museum-card">
            <div class="card-body">
                <h5 class="card-title">Выставки</h5>
                <p class="card-text">Просмотрите все прошедшие и текущие выставки музея.</p>
                <a asp-controller="Exhibitions" asp-action="Index" class="btn btn-primary">Выставки</a>
            </div>
        </div>
        <div class="museum-card">
            <div class="card-body">
                <h5 class="card-title">Категории</h5>
                <p class="card-text">Систематизация экспонатов по категориям для удобства поиска.</p>
                <a asp-controller="Categories" asp-action="Index" class="btn btn-primary">Категории</a>
            </div>
        </div>
    </div>
</div>