@model fly.Models.Movement

@{
    ViewData["Title"] = "Добавить перемещение";
}

<div class="container" style="margin-top: 20px; background-color: #f9f1e2;">
    <h1 class="my-4" style="color: #8b5522; text-align: center;">Добавить перемещение</h1>

    <hr />
    <div class="row">
        <div class="col-md-4">
            <form asp-action="Create" method="post" onsubmit="console.log('Form submitted'); return true;">
                @Html.AntiForgeryToken()
                <div asp-validation-summary="ModelOnly" class="text-danger"></div>
                <div class="form-group">
                    <label asp-for="ExhibitId" class="control-label"></label>
                    <select asp-for="ExhibitId" class="form-control" asp-items="ViewBag.ExhibitId">
                        <option value="">-- Выберите экспонат --</option>
                    </select>
                    <span asp-validation-for="ExhibitId" class="text-danger"></span>
                </div>
                <div class="form-group">
                    <label asp-for="FromStorageLocationId" class="control-label"></label>
                    <select asp-for="FromStorageLocationId" class="form-control" asp-items="ViewBag.FromStorageLocationId">
                        <option value="">-- Выберите место --</option>
                    </select>
                    <span asp-validation-for="FromStorageLocationId" class="text-danger"></span>
                </div>
                <div class="form-group">
                    <label asp-for="ToStorageLocationId" class="control-label"></label>
                    <select asp-for="ToStorageLocationId" class="form-control" asp-items="ViewBag.ToStorageLocationId">
                        <option value="">-- Выберите место --</option>
                    </select>
                    <span asp-validation-for="ToStorageLocationId" class="text-danger"></span>
                </div>
                <div class="form-group">
                    <label asp-for="MovementDate" class="control-label"></label>
                    <input asp-for="MovementDate" class="form-control" type="datetime-local" required />
                    <span asp-validation-for="MovementDate" class="text-danger"></span>
                </div>
                <div class="form-group">
                    <label asp-for="Notes" class="control-label"></label>
                    <input asp-for="Notes" class="form-control" />
                    <span asp-validation-for="Notes" class="text-danger"></span>
                </div>
                <div class="form-group">
                    <input type="submit" value="Добавить" class="btn btn-primary" style="background-color: #8b5522; border-color: #8b5522;" />
                </div>
            </form>
        </div>
    </div>
    <div>
        <a asp-action="Index" class="btn btn-secondary" style="background-color: #7f8c8d; border-color: #7f8c8d;">Вернуться к списку</a>
    </div>
</div>

@section Scripts {
    @{
        await Html.RenderPartialAsync("_ValidationScriptsPartial");
    }
    <script>
        document.addEventListener('DOMContentLoaded', function () {
            console.log('Page loaded. ViewBag.ExhibitId:', '@ViewBag.ExhibitId');
            console.log('Validation scripts loaded:', typeof jQuery !== 'undefined' && typeof jQuery.validator !== 'undefined');
        });
    </script>
}