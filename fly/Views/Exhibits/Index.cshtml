@model IEnumerable<fly.Models.Exhibit>


@{
    ViewData["Title"] = $"Экспонаты из категории: {ViewBag.CategoryName}";
}

<h1>Категории: @ViewBag.CategoryName</h1>


<p>
    <a asp-action="Create" asp-route-brandId="@ViewBag.CategoryId" class="btn btn-primary">Добавить новый экспонат</a>
</p>

<div class="row">
    @foreach (var item in Model)
    {
        <div class="col-md-3 mb-4">
            <div class="exhibit-model-card position-relative">
                @if (!string.IsNullOrEmpty(item.LogoPath))
                {
                    <div class="exhibit-model-logo-container">
                        <img src="@item.LogoPath" alt="Логотип" class="img-fluid exhibit-model-logo" />
                    </div>
                }
                <div class="exhibit-model-info position-absolute top-0 start-0 w-100 h-100 d-flex flex-column justify-content-center align-items-center text-center">
                    <h5>@item.ExhibitName</h5>
                    <a asp-action="Index" asp-controller="ExhibitInExhibitions" asp-route-id="@item.ExhibitId" class="btn btn-sm btn-primary">Экспонат на выставке</a>
                    <a asp-action="Edit" asp-route-id="@item.ExhibitId" asp-route-CategoryId="@ViewBag.CategoryId" class="btn btn-sm btn-warning mb-2">Редактировать</a>
                    <a asp-action="Delete" asp-route-id="@item.ExhibitId" asp-route-CategoryId="@ViewBag.CategoryId" class="btn btn-sm btn-danger mb-2">Удалить</a>
                </div>
            </div>
        </div>
    }
</div>
<p>
    <a asp-action="Index" asp-controller="Categories" class="btn btn-secondary">Назад</a>
</p>
@section Styles {
    <style>
        .exhibit-model-card {
            overflow: hidden;
            border: 1px solid #ddd;
            border-radius: 5px;
            transition: transform 0.3s;
            position: relative;
            height: 200px; /* Фиксированная высота для карточек */
        }

            .exhibit-model-card:hover {
                transform: scale(1.05);
            }

        .exhibit-model-logo-container {
            width: 100%;
            height: 100%;
            display: flex;
            justify-content: center;
            align-items: center;
            overflow: hidden;
        }

        .exhibit-model-logo {
            max-width: 100%;
            max-height: 100%;
            transition: filter 0.3s;
        }

        .exhibit-model-card:hover .exhibit-model-logo {
            filter: blur(5px);
        }

        .exhibit-model-info {
            background-color: rgba(255, 255, 255, 0.8);
            opacity: 0;
            transition: opacity 0.3s;
        }

        .exhibit-model-card:hover .exhibit-model-info {
            opacity: 1;
        }
    </style>
}